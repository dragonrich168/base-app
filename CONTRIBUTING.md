# Contributing

This project is built in public. Small, named commits are preferred.

1. Keep the default branch working.
2. Add one idea per commit.
3. Use the in-app changelog for user-facing slices.
4. Do not commit secrets or `.env` files.
