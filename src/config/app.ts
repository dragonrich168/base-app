import { APP_DESCRIPTION, APP_NAME, APP_TAGLINE } from "../constants/copy";

export const appConfig = {
  name: APP_NAME,
  tagline: APP_TAGLINE,
  description: APP_DESCRIPTION,
  repoUrl: "https://github.com/dragonrich168/base-app",
  version: "0.1.0",
} as const;
